class QCM:
    def __init__(self, question: str, possibles_responses: list(str), valids_reponses: list(str)) -> None:
        self.question = question
        self.possibles_responses = possibles_responses
        self.valids_reponses = valids_reponses
    def display():
        
        return 